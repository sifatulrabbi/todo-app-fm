import styled from 'styled-components';

export const Container = styled.form`
    width: 100%;
    height: max-content;

    .addTodo__input {
        width: 100%;
        padding: 0.6rem;
        font-size: 0.9rem;
        background-color: gray;
    }
`;
